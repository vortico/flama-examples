[pytest]
minversion = 3
norecursedirs = *settings* *urls* *docs*
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    type_integration: marks tests as integration (deselect with '-m "not type_integration"')
    type_unit: marks tests as unit (deselect with '-m "not type_unit"')
